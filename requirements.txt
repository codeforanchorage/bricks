# Brick OCR test pipeline -- Python dependencies
#
# IMPORTANT: install and run this project under Python 3.11, 3.12, or 3.13.
# paddlepaddle publishes NO wheels for Python 3.14, so PaddleOCR cannot run
# there. See README.md for environment setup.

# --- Method 2: Claude Sonnet (Anthropic API) --------------------------------
anthropic>=0.40.0
Pillow>=11.0.0
pillow-heif>=0.18.0     # .heic support (iPhone photos)

# --- Methods 3 & 4: Gemini 2.5 Pro / Flash (Google Gemini API) ---------------
google-genai>=1.0.0

# --- Tests (no API calls; see tests/ and README) -----------------------------
pytest>=8.0

# --- Reference data: parse the Municipality brick lists (PDF / .xls) -------
pypdfium2>=4.0.0
xlrd>=2.0.0

# --- Method 1: PaddleOCR (local OCR) ---------------------------------------
paddleocr>=3.0.0
#
# paddleocr does NOT pull in the paddlepaddle runtime -- install it separately,
# matching your hardware. Pick ONE of:
#
#   GPU build (CUDA): see https://www.paddlepaddle.org.cn/en/install/quick
#       for the exact command/index URL for your CUDA version, e.g.
#       pip install paddlepaddle-gpu==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#
#   CPU build (always works, fine for small test batches):
#       pip install paddlepaddle
